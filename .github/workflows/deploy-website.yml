name: 🚀 Deploy to CloudShield.me

on:
  push:
    branches:
      - main
    paths:
      - 'app/ui/src/**'
      - 'app/ui/public/**'
      - 'app/ui/index.html'
      - 'app/ui/vite.config.*'
      - 'app/ui/package.json'
      - 'app/api/**'
      - 'app/modules/**'
      - 'app/config/**'
      - 'app/core/**'
      - 'app/database/**'
      - 'docs/**'
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy to EC2
    runs-on: ubuntu-latest

    steps:

      # ── 1. Checkout ─────────────────────────────────────────────────────────
      - name: 📥 Checkout source
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ── 2. Setup SSH key ────────────────────────────────────────────────────
      - name: 🔑 Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/deploy_key.pem
          chmod 600 ~/.ssh/deploy_key.pem
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      # ── 3. Build React frontend ──────────────────────────────────────────────
      - name: 🟢 Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: app/ui/package-lock.json

      - name: 📦 Install frontend dependencies
        run: cd app/ui && npm ci

      - name: 🔨 Build React (production)
        run: cd app/ui && npm run build
        env:
          VITE_API_URL: https://cloudshield.me
          VITE_WEB_MODE: true

      # ── 4. Package everything into one tar file ──────────────────────────────
      - name: 📦 Package files for deployment
        run: |
          tar -czf /tmp/cloudshield-deploy.tar.gz \
            app/ui/dist \
            app/api \
            app/modules \
            app/config \
            app/core \
            app/database \
            docs
          echo "Package size: $(du -sh /tmp/cloudshield-deploy.tar.gz)"

      # ── 5. Upload single tar file via SCP ────────────────────────────────────
      - name: 📤 Upload to EC2
        run: |
          scp -q -i ~/.ssh/deploy_key.pem \
            -o StrictHostKeyChecking=no \
            -o LogLevel=ERROR \
            /tmp/cloudshield-deploy.tar.gz \
            root@${{ secrets.EC2_HOST }}:/tmp/cloudshield-deploy.tar.gz

      # ── 6. Extract + apply + restart on EC2 ─────────────────────────────────
      - name: 🔄 Apply changes & restart service
        run: |
          ssh -q -i ~/.ssh/deploy_key.pem \
            -o StrictHostKeyChecking=no \
            -o LogLevel=ERROR \
            root@${{ secrets.EC2_HOST }} << 'EOF'
          set -e

          echo "📦 Extracting deployment package..."
          cd /tmp
          tar -xzf cloudshield-deploy.tar.gz
          rm cloudshield-deploy.tar.gz

          echo "📁 Deploying frontend..."
          cp -rf /tmp/app/ui/dist/* /root/cloudshield/app/ui/dist/
          echo "✅ Frontend deployed"

          echo "📁 Deploying backend..."
          cp -rf /tmp/app/api       /root/cloudshield/app/
          cp -rf /tmp/app/modules   /root/cloudshield/app/
          cp -rf /tmp/app/config    /root/cloudshield/app/
          cp -rf /tmp/app/core      /root/cloudshield/app/
          cp -rf /tmp/app/database  /root/cloudshield/app/
          cp -rf /tmp/docs          /root/cloudshield/
          echo "✅ Backend deployed"

          echo "🧹 Cleaning up..."
          rm -rf /tmp/app

          echo "🔄 Restarting CloudShield service..."
          systemctl restart cloudshield
          sleep 5

          systemctl is-active cloudshield \
            && echo "🎉 CloudShield.me is live!" \
            || (echo "❌ Service failed!" && journalctl -u cloudshield -n 20 --no-pager && exit 1)
          EOF

      - name: ✅ Done
        run: echo "🚀 CloudShield.me deployed successfully!"
